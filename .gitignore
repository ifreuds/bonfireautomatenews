node_modules
.next
.env
.env.local
.vercel

# --- MCP server: deps + the agent token (NEVER commit) ---
mcp/node_modules/
mcp/.token

# --- scratch / temp ---
_pipeline_tmp/

# --- build / upload artifacts ---
*.zip
